spring:
    application:
        name: apigateway
    cloud:
        gateway:
            routes:
#                - id: auth
#                  uri: localhost:8081
#                  predicates:
#                  - Path=/api/auth/**
#                  - Path=/api/test/**
                - id: test
                  uri: localhost:8086
                  predicates:
                      - Path=/test
                - id: auth
                  uri: localhost:8081
                  predicates:
                        - Path=/api/v1/auth/**
                  filters:
                      - AuthFilter
#            mvc:
#                routes:
#                    - id: auth
#                      uri: localhost:8081
#                      predicates:
#                          - Path=/api/v1/auth/**
#                    - id: test
#                      uri: localhost:8082
server:
    port: 9000

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka
        fetch-registry: true
        register-with-eureka: true

logging:
    level:
        root: INFO


